tupla_vazia = ()

tupla2 = ('a', 'b', 'c')

vogais = ('a', 'e', 'i', 'o', 'u')

print(f"Tipo de objeto voagais = {type(vogais)}")

for p, x in enumerate(vogais):
    print(f"Posição = {p}, Valor = {x}")